% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{volcano_plot}
\alias{volcano_plot}
\title{Create an elegant volcano plot}
\usage{
volcano_plot(
  data,
  x,
  y,
  label = NULL,
  color = NULL,
  x_cutoff = NULL,
  y_cutoff = NULL,
  ...,
  title = NULL,
  subtitle = NULL,
  palette = "aurora",
  point_size = 1.55,
  point_alpha = 0.78,
  label_size = 3.4,
  label_color = "#1F2937",
  label_segment_color = "#9AA4B2",
  label_segment_size = 0.32,
  label_segment_alpha = 0.88,
  label_point_ring_halo_color = "#E5E7EB",
  label_point_ring_halo_alpha = 0.93,
  label_point_ring_halo_size = 3.45,
  label_point_ring_halo_stroke = 1.15,
  label_point_ring_color = "auto",
  label_point_ring_normal_color = "auto",
  label_point_ring_alpha = 0.95,
  label_point_ring_size = 2.75,
  label_point_ring_stroke = 0.85,
  label_wrap_width = "auto",
  label_max_lines = "auto",
  label_anchor_x_left = NULL,
  label_anchor_x_right = NULL,
  plot_xmax = NULL,
  ymax = NULL,
  base_size = 12,
  legend_position = "top",
  xlab = NULL,
  ylab = NULL
)
}
\arguments{
\item{data}{A data frame containing plotting-ready columns.}

\item{x, y}{Column names for the x and y positions. For a typical volcano
plot these are log2 fold change and \code{-log10(p)}.}

\item{label}{Optional column containing labels to draw. \code{NA} and empty
strings are ignored.}

\item{color}{Optional column used for point groups and colors.}

\item{x_cutoff}{Optional positive x cutoff for symmetric vertical guide
lines. This is only a guide; it does not classify rows.}

\item{y_cutoff}{Optional y cutoff for a horizontal guide line. This is only
a guide; it does not classify rows.}

\item{...}{Unused arguments are rejected with a clear error.}

\item{title, subtitle}{Plot title and subtitle.}

\item{palette}{Named color vector or palette name passed to
\code{\link[=volcano_palette]{volcano_palette()}}. Custom color groups are supported with named vectors.}

\item{point_size, point_alpha}{Main point size and alpha.}

\item{label_size, label_color}{Label text controls.}

\item{label_segment_color, label_segment_size, label_segment_alpha}{Connector
controls.}

\item{label_point_ring_halo_color}{Color for the outer contrast halo around
each labelled source point ring.}

\item{label_point_ring_halo_alpha, label_point_ring_halo_size, label_point_ring_halo_stroke}{Alpha, size, and stroke width for the outer contrast halo.}

\item{label_point_ring_color}{Color for the ring drawn around each labelled
source point. Use \code{"auto"} to derive higher-contrast ring colors from
each point group color, or \code{"group"} to follow group colors exactly.}

\item{label_point_ring_normal_color}{Ring color used for \code{Normal} labelled
points when \code{label_point_ring_color = "auto"}. The default \code{"auto"}
derives a higher-contrast color from the current \code{Normal} point color.
Set a color value to override only the \code{Normal} ring color.}

\item{label_point_ring_alpha, label_point_ring_size, label_point_ring_stroke}{Alpha, size, and stroke width for the inner labelled source point rings.}

\item{label_wrap_width}{Maximum label line width. Use \code{"auto"} to choose
from label length and density.}

\item{label_max_lines}{Maximum number of lines per label before capping with
an ellipsis. Use \code{"auto"} to allow more detail only when labels are sparse.}

\item{label_anchor_x_left, label_anchor_x_right}{Optional x positions for the
left and right outside label anchor columns. Leave as \code{NULL} for automatic
placement.}

\item{plot_xmax}{Optional x-axis half-width.}

\item{ymax}{Optional y-axis maximum.}

\item{base_size}{Base font size.}

\item{legend_position}{Legend position.}

\item{xlab, ylab}{Axis labels.}
}
\value{
A ggplot object.
}
\description{
Builds a publication-ready volcano plot from already prepared plotting
columns. The function does not read files, transform p-values, classify
genes, or choose labels. It focuses on the drawing layer: soft points,
threshold guides, counted legends, and adaptive outside label columns.
}
